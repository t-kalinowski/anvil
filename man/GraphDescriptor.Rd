% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph.R
\name{GraphDescriptor}
\alias{GraphDescriptor}
\title{Graph Descriptor}
\usage{
GraphDescriptor(
  calls = list(),
  tensor_to_gval = NULL,
  gval_to_box = NULL,
  constants = list(),
  in_tree = NULL,
  out_tree = NULL,
  inputs = list(),
  outputs = list(),
  is_static_flat = NULL,
  static_args_flat = NULL,
  devices = character()
)
}
\arguments{
\item{calls}{(\code{list(PrimitiveCall)})\cr
The primitive calls that make up the graph.}

\item{tensor_to_gval}{(\code{hashtab})\cr
Mapping: \code{AnvilTensor} -> \code{GraphValue}}

\item{gval_to_box}{(\code{hashtab})\cr
Mapping: \code{GraphValue} -> \code{GraphBox}}

\item{constants}{(\code{list(GraphValue)})\cr
The constants of the graph.}

\item{in_tree}{(\code{NULL | Node})\cr
The tree of inputs. May contain leaves for both tensor inputs and static
(non-tensor) arguments. Only the tensor leaves correspond to entries in
\code{inputs}; use \code{is_static_flat} to distinguish them.}

\item{out_tree}{(\code{NULL | Node})\cr
The tree of outputs.}

\item{inputs}{(\code{list(GraphValue)})\cr
The inputs to the graph (tensor arguments only).}

\item{outputs}{(\code{list(GraphValue)})\cr
The outputs of the graph.}

\item{is_static_flat}{(\code{NULL | logical()})\cr
Boolean mask indicating which flat positions in \code{in_tree} are static (non-tensor) args.
\code{NULL} when all args are tensor inputs.}

\item{static_args_flat}{(\code{NULL | list()})\cr
Flattened traced values for the static arguments indicated by \code{is_static_flat}.}

\item{devices}{(\code{character()})\cr
Device platforms encountered during tracing (e.g. \code{"cpu"}, \code{"cuda"}).
Populated automatically as tensors are registered.}
}
\value{
(\code{GraphDescriptor})
}
\description{
Descriptor of an \code{\link{AnvilGraph}}. This is a mutable class.
}
